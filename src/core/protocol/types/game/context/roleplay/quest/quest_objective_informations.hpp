//  Generated by desperion protocol_builder

#ifndef core_quest_objective_informations_hpp
#define core_quest_objective_informations_hpp

namespace protocol
{
    struct quest_objective_informations : dofus_unit
    {
        int16_t objective_id;
        bool objective_status;
        std::vector<std::string> dialog_params;
        
        int16_t id() const override
        { return 385; } 
        
        quest_objective_informations(const int16_t & objective_id, const bool & objective_status, const std::vector<std::string> & dialog_params)
        {
            _data << objective_id;
            _data << objective_status;
            _data << static_cast<int16_t>(dialog_params.size());
            for (auto && i : dialog_params)
                _data << i;
        }
        
        quest_objective_informations(byte_buffer & data)
        {
            data >> objective_id;
            data >> objective_status;
            int16_t dialog_params_size;
            data >> dialog_params_size;
            dialog_params.resize(dialog_params_size);
            for (int16_t i = 0; i < dialog_params_size; ++i)
                data >> dialog_params[i];
        }
    };
    
    using quest_objective_informations_ptr = std::unique_ptr<quest_objective_informations>;
}

#endif
