//  Generated by desperion protocol_builder

#ifndef core_servers_list_message_hpp
#define core_servers_list_message_hpp

namespace network
{
    struct servers_list_message : dofus_unit
    {
        std::vector<game_server_informations_ptr> servers;
        
        int16_t id() const override
        { return 30; } 
        
        servers_list_message(const std::vector<game_server_informations_ptr> & servers)
        {
            _data << static_cast<int16_t>(servers.size());
            for (auto && i : servers)
                _data << i->buffer();
        }
        
        servers_list_message(byte_buffer & data)
        {
            int16_t servers_size;
            data >> servers_size;
            servers.resize(servers_size);
            for (int16_t i = 0; i < servers_size; ++i)
                servers[i] = std::make_unique<game_server_informations>(data);
        }
    };
}

#endif
