//  Generated by desperion protocol_builder

#ifndef core_new_mail_message_hpp
#define core_new_mail_message_hpp

namespace network
{
    struct new_mail_message : mail_status_message
    {
        std::vector<int32_t> senders_account_id;
        
        int16_t id() const override
        { return 6292; } 
        
        new_mail_message(const int16_t & unread, const int16_t & total, const std::vector<int32_t> & senders_account_id) : mail_status_message { unread,total }
        {
            _data << static_cast<int16_t>(senders_account_id.size());
            for (auto && i : senders_account_id)
                _data << i;
        }
        
        new_mail_message(byte_buffer & data) : mail_status_message { data }
        {
            int16_t senders_account_id_size;
            data >> senders_account_id_size;
            senders_account_id.resize(senders_account_id_size);
            for (int16_t i = 0; i < senders_account_id_size; ++i)
                data >> senders_account_id[i];
        }
    };
}

#endif
