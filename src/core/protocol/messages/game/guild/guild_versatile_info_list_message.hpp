//  Generated by desperion protocol_builder

#ifndef core_guild_versatile_info_list_message_hpp
#define core_guild_versatile_info_list_message_hpp

namespace protocol
{
    struct guild_versatile_info_list_message : dofus_unit
    {
        std::vector<guild_versatile_informations_ptr> guilds;
        
        int16_t id() const override
        { return 6435; } 
        
        guild_versatile_info_list_message(const std::vector<guild_versatile_informations_ptr> & guilds)
        {
            _data << static_cast<int16_t>(guilds.size());
            for (auto && i : guilds)
                _data << i->id() << i->buffer();
        }
        
        guild_versatile_info_list_message(byte_buffer & data)
        {
            int16_t guilds_size;
            data >> guilds_size;
            guilds.resize(guilds_size);
            for (int16_t i = 0; i < guilds_size; ++i)
            {
                int16_t p;
                data >> p;
                guilds[i].reset(type_manager::get_guild_versatile_informations(p, data));
            }
        }
    };
}

#endif
