//  Generated by desperion protocol_builder

#ifndef core_server_optional_features_message_hpp
#define core_server_optional_features_message_hpp

namespace network
{
    struct server_optional_features_message : dofus_unit
    {
        std::vector<int16_t> features;
        
        int16_t id() const override
        { return 6305; } 
        
        server_optional_features_message(const std::vector<int16_t> & features)
        {
            _data << static_cast<int16_t>(features.size());
            for (auto && i : features)
                _data << i;
        }
        
        server_optional_features_message(byte_buffer & data)
        {
            int16_t features_size;
            data >> features_size;
            features.resize(features_size);
            for (int16_t i = 0; i < features_size; ++i)
                data >> features[i];
        }
    };
}

#endif
