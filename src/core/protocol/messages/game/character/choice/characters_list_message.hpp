//  Generated by desperion protocol_builder

#ifndef core_characters_list_message_hpp
#define core_characters_list_message_hpp

namespace protocol
{
    struct characters_list_message : dofus_unit
    {
        bool has_startup_actions;
        std::vector<character_base_informations_ptr> characters;
        
        int16_t id() const override
        { return 151; } 
        
        characters_list_message(const bool & has_startup_actions, const std::vector<character_base_informations_ptr> & characters)
        {
            _data << has_startup_actions;
            _data << static_cast<int16_t>(characters.size());
            for (auto && i : characters)
                _data << i->id() << i->buffer();
        }
        
        characters_list_message(byte_buffer & data)
        {
            data >> has_startup_actions;
            int16_t characters_size;
            data >> characters_size;
            characters.resize(characters_size);
            for (int16_t i = 0; i < characters_size; ++i)
            {
                int16_t p;
                data >> p;
                characters[i].reset(type_manager::get_character_base_informations(p, data));
            }
        }
    };
}

#endif
