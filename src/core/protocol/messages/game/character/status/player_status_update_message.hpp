//  Generated by desperion protocol_builder

#ifndef core_player_status_update_message_hpp
#define core_player_status_update_message_hpp

namespace protocol
{
    struct player_status_update_message : dofus_unit
    {
        int32_t account_id;
        int32_t player_id;
        player_status_ptr status;
        
        int16_t id() const override
        { return 6386; } 
        
        player_status_update_message(const int32_t & account_id, const int32_t & player_id, const player_status_ptr & status)
        {
            _data << account_id;
            _data << player_id;
            _data << status->id() << status->buffer();
        }
        
        player_status_update_message(byte_buffer & data)
        {
            data >> account_id;
            data >> player_id;
            int16_t status_id;
            data >> status_id;
            status.reset(type_manager::get_player_status(status_id, data));
        }
    };
}

#endif
