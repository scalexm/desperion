//  Generated by desperion protocol_builder

#ifndef core_game_action_fight_tackled_message_hpp
#define core_game_action_fight_tackled_message_hpp

namespace protocol
{
    struct game_action_fight_tackled_message : abstract_game_action_message
    {
        std::vector<int32_t> tacklers_ids;
        
        int16_t id() const override
        { return 1004; } 
        
        game_action_fight_tackled_message(const int16_t & action_id, const int32_t & source_id, const std::vector<int32_t> & tacklers_ids) : abstract_game_action_message { action_id,source_id }
        {
            _data << static_cast<int16_t>(tacklers_ids.size());
            for (auto && i : tacklers_ids)
                _data << i;
        }
        
        game_action_fight_tackled_message(byte_buffer & data) : abstract_game_action_message { data }
        {
            int16_t tacklers_ids_size;
            data >> tacklers_ids_size;
            tacklers_ids.resize(tacklers_ids_size);
            for (int16_t i = 0; i < tacklers_ids_size; ++i)
                data >> tacklers_ids[i];
        }
    };
}

#endif
