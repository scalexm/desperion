//  Generated by desperion protocol_builder

#ifndef core_teleport_buddies_requested_message_hpp
#define core_teleport_buddies_requested_message_hpp

namespace protocol
{
    struct teleport_buddies_requested_message : dofus_unit
    {
        int16_t dungeon_id;
        int32_t inviter_id;
        std::vector<int32_t> invalid_buddies_ids;
        
        int16_t id() const override
        { return 6302; } 
        
        teleport_buddies_requested_message(const int16_t & dungeon_id, const int32_t & inviter_id, const std::vector<int32_t> & invalid_buddies_ids)
        {
            _data << dungeon_id;
            _data << inviter_id;
            _data << static_cast<int16_t>(invalid_buddies_ids.size());
            for (auto && i : invalid_buddies_ids)
                _data << i;
        }
        
        teleport_buddies_requested_message(byte_buffer & data)
        {
            data >> dungeon_id;
            data >> inviter_id;
            int16_t invalid_buddies_ids_size;
            data >> invalid_buddies_ids_size;
            invalid_buddies_ids.resize(invalid_buddies_ids_size);
            for (int16_t i = 0; i < invalid_buddies_ids_size; ++i)
                data >> invalid_buddies_ids[i];
        }
    };
}

#endif
