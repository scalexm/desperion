//  Generated by desperion protocol_builder

#ifndef core_shortcut_bar_content_message_hpp
#define core_shortcut_bar_content_message_hpp

namespace network
{
    struct shortcut_bar_content_message : dofus_unit
    {
        int8_t bar_type;
        std::vector<shortcut_ptr> shortcuts;
        
        int16_t id() const override
        { return 6231; } 
        
        shortcut_bar_content_message(const int8_t & bar_type, const std::vector<shortcut_ptr> & shortcuts)
        {
            _data << bar_type;
            _data << static_cast<int16_t>(shortcuts.size());
            for (auto && i : shortcuts)
                _data << i->id() << i->buffer();
        }
        
        shortcut_bar_content_message(byte_buffer & data)
        {
            data >> bar_type;
            int16_t shortcuts_size;
            data >> shortcuts_size;
            shortcuts.resize(shortcuts_size);
            for (int16_t i = 0; i < shortcuts_size; ++i)
            {
                int16_t p;
                data >> p;
                shortcuts[i].reset(type_manager::get_shortcut(p, data));
            }
        }
    };
}

#endif
