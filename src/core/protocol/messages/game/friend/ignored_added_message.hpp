//  Generated by desperion protocol_builder

#ifndef core_ignored_added_message_hpp
#define core_ignored_added_message_hpp

namespace protocol
{
    struct ignored_added_message : dofus_unit
    {
        ignored_informations_ptr ignore_added;
        bool session;
        
        int16_t id() const override
        { return 5678; } 
        
        ignored_added_message(const ignored_informations_ptr & ignore_added, const bool & session)
        {
            _data << ignore_added->id() << ignore_added->buffer();
            _data << session;
        }
        
        ignored_added_message(byte_buffer & data)
        {
            int16_t ignore_added_id;
            data >> ignore_added_id;
            ignore_added.reset(type_manager::get_ignored_informations(ignore_added_id, data));
            data >> session;
        }
    };
}

#endif
