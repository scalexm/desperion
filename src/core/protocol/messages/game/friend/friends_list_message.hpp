//  Generated by desperion protocol_builder

#ifndef core_friends_list_message_hpp
#define core_friends_list_message_hpp

namespace protocol
{
    struct friends_list_message : dofus_unit
    {
        std::vector<friend_informations_ptr> friends_list;
        
        int16_t id() const override
        { return 4002; } 
        
        friends_list_message(const std::vector<friend_informations_ptr> & friends_list)
        {
            _data << static_cast<int16_t>(friends_list.size());
            for (auto && i : friends_list)
                _data << i->id() << i->buffer();
        }
        
        friends_list_message(byte_buffer & data)
        {
            int16_t friends_list_size;
            data >> friends_list_size;
            friends_list.resize(friends_list_size);
            for (int16_t i = 0; i < friends_list_size; ++i)
            {
                int16_t p;
                data >> p;
                friends_list[i].reset(type_manager::get_friend_informations(p, data));
            }
        }
    };
}

#endif
