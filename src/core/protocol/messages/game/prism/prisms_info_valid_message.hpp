//  Generated by desperion protocol_builder

#ifndef core_prisms_info_valid_message_hpp
#define core_prisms_info_valid_message_hpp

namespace protocol
{
    struct prisms_info_valid_message : dofus_unit
    {
        std::vector<prism_fighters_information_ptr> fights;
        
        int16_t id() const override
        { return 6451; } 
        
        prisms_info_valid_message(const std::vector<prism_fighters_information_ptr> & fights)
        {
            _data << static_cast<int16_t>(fights.size());
            for (auto && i : fights)
                _data << i->buffer();
        }
        
        prisms_info_valid_message(byte_buffer & data)
        {
            int16_t fights_size;
            data >> fights_size;
            fights.resize(fights_size);
            for (int16_t i = 0; i < fights_size; ++i)
                fights[i] = std::make_unique<prism_fighters_information>(data);
        }
    };
}

#endif
