//  Generated by desperion protocol_builder

#ifndef core_object_ground_removed_multiple_message_hpp
#define core_object_ground_removed_multiple_message_hpp

namespace protocol
{
    struct object_ground_removed_multiple_message : dofus_unit
    {
        std::vector<int16_t> cells;
        
        int16_t id() const override
        { return 5944; } 
        
        object_ground_removed_multiple_message(const std::vector<int16_t> & cells)
        {
            _data << static_cast<int16_t>(cells.size());
            for (auto && i : cells)
                _data << i;
        }
        
        object_ground_removed_multiple_message(byte_buffer & data)
        {
            int16_t cells_size;
            data >> cells_size;
            cells.resize(cells_size);
            for (int16_t i = 0; i < cells_size; ++i)
                data >> cells[i];
        }
    };
}

#endif
