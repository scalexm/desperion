//  Generated by desperion protocol_builder

#ifndef core_entity_talk_message_hpp
#define core_entity_talk_message_hpp

namespace protocol
{
    struct entity_talk_message : dofus_unit
    {
        int32_t entity_id;
        int16_t text_id;
        std::vector<std::string> parameters;
        
        int16_t id() const override
        { return 6110; } 
        
        entity_talk_message(const int32_t & entity_id, const int16_t & text_id, const std::vector<std::string> & parameters)
        {
            _data << entity_id;
            _data << text_id;
            _data << static_cast<int16_t>(parameters.size());
            for (auto && i : parameters)
                _data << i;
        }
        
        entity_talk_message(byte_buffer & data)
        {
            data >> entity_id;
            data >> text_id;
            int16_t parameters_size;
            data >> parameters_size;
            parameters.resize(parameters_size);
            for (int16_t i = 0; i < parameters_size; ++i)
                data >> parameters[i];
        }
    };
}

#endif
