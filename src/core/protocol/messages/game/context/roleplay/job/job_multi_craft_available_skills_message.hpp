//  Generated by desperion protocol_builder

#ifndef core_job_multi_craft_available_skills_message_hpp
#define core_job_multi_craft_available_skills_message_hpp

namespace protocol
{
    struct job_multi_craft_available_skills_message : job_allow_multi_craft_request_message
    {
        int32_t player_id;
        std::vector<int16_t> skills;
        
        int16_t id() const override
        { return 5747; } 
        
        job_multi_craft_available_skills_message(const bool & enabled, const int32_t & player_id, const std::vector<int16_t> & skills) : job_allow_multi_craft_request_message { enabled }
        {
            _data << player_id;
            _data << static_cast<int16_t>(skills.size());
            for (auto && i : skills)
                _data << i;
        }
        
        job_multi_craft_available_skills_message(byte_buffer & data) : job_allow_multi_craft_request_message { data }
        {
            data >> player_id;
            int16_t skills_size;
            data >> skills_size;
            skills.resize(skills_size);
            for (int16_t i = 0; i < skills_size; ++i)
                data >> skills[i];
        }
    };
}

#endif
