//  Generated by desperion protocol_builder

#ifndef core_map_obstacle_update_message_hpp
#define core_map_obstacle_update_message_hpp

namespace protocol
{
    struct map_obstacle_update_message : dofus_unit
    {
        std::vector<map_obstacle_ptr> obstacles;
        
        int16_t id() const override
        { return 6051; } 
        
        map_obstacle_update_message(const std::vector<map_obstacle_ptr> & obstacles)
        {
            _data << static_cast<int16_t>(obstacles.size());
            for (auto && i : obstacles)
                _data << i->buffer();
        }
        
        map_obstacle_update_message(byte_buffer & data)
        {
            int16_t obstacles_size;
            data >> obstacles_size;
            obstacles.resize(obstacles_size);
            for (int16_t i = 0; i < obstacles_size; ++i)
                obstacles[i] = std::make_unique<map_obstacle>(data);
        }
    };
}

#endif
