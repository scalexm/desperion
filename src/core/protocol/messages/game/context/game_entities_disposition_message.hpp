//  Generated by desperion protocol_builder

#ifndef core_game_entities_disposition_message_hpp
#define core_game_entities_disposition_message_hpp

namespace protocol
{
    struct game_entities_disposition_message : dofus_unit
    {
        std::vector<identified_entity_disposition_informations_ptr> dispositions;
        
        int16_t id() const override
        { return 5696; } 
        
        game_entities_disposition_message(const std::vector<identified_entity_disposition_informations_ptr> & dispositions)
        {
            _data << static_cast<int16_t>(dispositions.size());
            for (auto && i : dispositions)
                _data << i->buffer();
        }
        
        game_entities_disposition_message(byte_buffer & data)
        {
            int16_t dispositions_size;
            data >> dispositions_size;
            dispositions.resize(dispositions_size);
            for (int16_t i = 0; i < dispositions_size; ++i)
                dispositions[i] = std::make_unique<identified_entity_disposition_informations>(data);
        }
    };
}

#endif
