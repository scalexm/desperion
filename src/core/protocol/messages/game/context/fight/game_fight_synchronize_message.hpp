//  Generated by desperion protocol_builder

#ifndef core_game_fight_synchronize_message_hpp
#define core_game_fight_synchronize_message_hpp

namespace protocol
{
    struct game_fight_synchronize_message : dofus_unit
    {
        std::vector<game_fight_fighter_informations_ptr> fighters;
        
        int16_t id() const override
        { return 5921; } 
        
        game_fight_synchronize_message(const std::vector<game_fight_fighter_informations_ptr> & fighters)
        {
            _data << static_cast<int16_t>(fighters.size());
            for (auto && i : fighters)
                _data << i->id() << i->buffer();
        }
        
        game_fight_synchronize_message(byte_buffer & data)
        {
            int16_t fighters_size;
            data >> fighters_size;
            fighters.resize(fighters_size);
            for (int16_t i = 0; i < fighters_size; ++i)
            {
                int16_t p;
                data >> p;
                fighters[i].reset(type_manager::get_game_fight_fighter_informations(p, data));
            }
        }
    };
}

#endif
