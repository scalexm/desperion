//  Generated by desperion protocol_builder

#ifndef core_character_creation_request_message_hpp
#define core_character_creation_request_message_hpp

namespace network
{
    struct character_creation_request_message : dofus_unit
    {
        std::string name;
        int8_t breed;
        bool sex;
        std::vector<int32_t> colors;
        int32_t cosmetic_id;
        
        int16_t id() const override
        { return 160; } 
        
        character_creation_request_message(const std::string & name, const int8_t & breed, const bool & sex, const std::vector<int32_t> & colors, const int32_t & cosmetic_id)
        {
            _data << name;
            _data << breed;
            _data << sex;
            _data << static_cast<int16_t>(colors.size());
            for (auto && i : colors)
                _data << i;
            _data << cosmetic_id;
        }
        
        character_creation_request_message(byte_buffer & data)
        {
            data >> name;
            data >> breed;
            data >> sex;
            int16_t colors_size;
            data >> colors_size;
            colors.resize(colors_size);
            for (int16_t i = 0; i < colors_size; ++i)
                data >> colors[i];
            data >> cosmetic_id;
        }
    };
}

#endif
