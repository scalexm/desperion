//  Generated by desperion protocol_builder

#ifndef core_exchange_object_transfert_list_to_inv_message_hpp
#define core_exchange_object_transfert_list_to_inv_message_hpp

namespace network
{
    struct exchange_object_transfert_list_to_inv_message : dofus_unit
    {
        std::vector<int32_t> ids;
        
        int16_t id() const override
        { return 6039; } 
        
        exchange_object_transfert_list_to_inv_message(const std::vector<int32_t> & ids)
        {
            _data << static_cast<int16_t>(ids.size());
            for (auto && i : ids)
                _data << i;
        }
        
        exchange_object_transfert_list_to_inv_message(byte_buffer & data)
        {
            int16_t ids_size;
            data >> ids_size;
            ids.resize(ids_size);
            for (int16_t i = 0; i < ids_size; ++i)
                data >> ids[i];
        }
    };
}

#endif
