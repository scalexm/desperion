//  Generated by desperion protocol_builder

#ifndef core_exchange_ready_message_hpp
#define core_exchange_ready_message_hpp

namespace network
{
    struct exchange_ready_message : dofus_unit
    {
        bool ready;
        int16_t step;
        
        int16_t id() const override
        { return 5511; } 
        
        exchange_ready_message(const bool & ready, const int16_t & step)
        {
            _data << ready;
            _data << step;
        }
        
        exchange_ready_message(byte_buffer & data)
        {
            data >> ready;
            data >> step;
        }
    };
}

#endif
