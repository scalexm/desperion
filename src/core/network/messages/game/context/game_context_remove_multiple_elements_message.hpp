//  Generated by desperion protocol_builder

#ifndef core_game_context_remove_multiple_elements_message_hpp
#define core_game_context_remove_multiple_elements_message_hpp

namespace network
{
    struct game_context_remove_multiple_elements_message : dofus_unit
    {
        std::vector<int32_t> id;
        
        int16_t id() const override
        { return 252; } 
        
        game_context_remove_multiple_elements_message(const std::vector<int32_t> & id)
        {
            _data << static_cast<int16_t>(id.size());
            for (auto && i : id)
                _data << i;
        }
        
        game_context_remove_multiple_elements_message(byte_buffer & data)
        {
            int16_t id_size;
            data >> id_size;
            id.resize(id_size);
            for (int16_t i = 0; i < id_size; ++i)
                data >> id[i];
        }
    };
}

#endif
