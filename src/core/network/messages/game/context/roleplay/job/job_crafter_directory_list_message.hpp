//  Generated by desperion protocol_builder

#ifndef core_job_crafter_directory_list_message_hpp
#define core_job_crafter_directory_list_message_hpp

namespace network
{
    struct job_crafter_directory_list_message : dofus_unit
    {
        std::vector<job_crafter_directory_list_entry_ptr> list_entries;
        
        int16_t id() const override
        { return 6046; } 
        
        job_crafter_directory_list_message(const std::vector<job_crafter_directory_list_entry_ptr> & list_entries)
        {
            _data << static_cast<int16_t>(list_entries.size());
            for (auto && i : list_entries)
                _data << i->buffer();
        }
        
        job_crafter_directory_list_message(byte_buffer & data)
        {
            int16_t list_entries_size;
            data >> list_entries_size;
            list_entries.resize(list_entries_size);
            for (int16_t i = 0; i < list_entries_size; ++i)
                list_entries[i] = std::make_unique<job_crafter_directory_list_entry>(data);
        }
    };
}

#endif
