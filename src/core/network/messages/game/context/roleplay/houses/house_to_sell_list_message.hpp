//  Generated by desperion protocol_builder

#ifndef core_house_to_sell_list_message_hpp
#define core_house_to_sell_list_message_hpp

namespace network
{
    struct house_to_sell_list_message : dofus_unit
    {
        int16_t page_index;
        int16_t total_page;
        std::vector<house_informations_for_sell_ptr> house_list;
        
        int16_t id() const override
        { return 6140; } 
        
        house_to_sell_list_message(const int16_t & page_index, const int16_t & total_page, const std::vector<house_informations_for_sell_ptr> & house_list)
        {
            _data << page_index;
            _data << total_page;
            _data << static_cast<int16_t>(house_list.size());
            for (auto && i : house_list)
                _data << i->buffer();
        }
        
        house_to_sell_list_message(byte_buffer & data)
        {
            data >> page_index;
            data >> total_page;
            int16_t house_list_size;
            data >> house_list_size;
            house_list.resize(house_list_size);
            for (int16_t i = 0; i < house_list_size; ++i)
                house_list[i] = std::make_unique<house_informations_for_sell>(data);
        }
    };
}

#endif
