//  Generated by desperion protocol_builder

#ifndef core_guild_informations_members_message_hpp
#define core_guild_informations_members_message_hpp

namespace network
{
    struct guild_informations_members_message : dofus_unit
    {
        std::vector<guild_member_ptr> members;
        
        int16_t id() const override
        { return 5558; } 
        
        guild_informations_members_message(const std::vector<guild_member_ptr> & members)
        {
            _data << static_cast<int16_t>(members.size());
            for (auto && i : members)
                _data << i->buffer();
        }
        
        guild_informations_members_message(byte_buffer & data)
        {
            int16_t members_size;
            data >> members_size;
            members.resize(members_size);
            for (int16_t i = 0; i < members_size; ++i)
                members[i] = std::make_unique<guild_member>(data);
        }
    };
}

#endif
