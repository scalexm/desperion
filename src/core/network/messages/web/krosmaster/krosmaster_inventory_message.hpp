//  Generated by desperion protocol_builder

#ifndef core_krosmaster_inventory_message_hpp
#define core_krosmaster_inventory_message_hpp

namespace network
{
    struct krosmaster_inventory_message : dofus_unit
    {
        std::vector<krosmaster_figure_ptr> figures;
        
        int16_t id() const override
        { return 6350; } 
        
        krosmaster_inventory_message(const std::vector<krosmaster_figure_ptr> & figures)
        {
            _data << static_cast<int16_t>(figures.size());
            for (auto && i : figures)
                _data << i->buffer();
        }
        
        krosmaster_inventory_message(byte_buffer & data)
        {
            int16_t figures_size;
            data >> figures_size;
            figures.resize(figures_size);
            for (int16_t i = 0; i < figures_size; ++i)
                figures[i] = std::make_unique<krosmaster_figure>(data);
        }
    };
}

#endif
