//  Generated by desperion protocol_builder

#ifndef d2fstream_achievement_objective_hpp
#define d2fstream_achievement_objective_hpp

namespace datacenter
{
    struct achievement_objective : data_holder
    {
        int32_t id;
        int32_t achievement_id;
        int32_t name_id;
        std::string criterion;
        
        achievement_objective() = default;
        
        achievement_objective(const int32_t & id, const int32_t & achievement_id, const int32_t & name_id, const std::string & criterion) : id { id }, achievement_id { achievement_id }, name_id { name_id }, criterion { criterion }
        {
        }
        
        achievement_objective(data_object && data)
        {
            acquire(id, data.second.at("id"));
            acquire(achievement_id, data.second.at("achievementId"));
            acquire(name_id, data.second.at("nameId"));
            acquire(criterion, data.second.at("criterion"));
        }
        
        data_object to_d2o(const d2o_writer &);
    };

#ifdef d2fstream_d2o_writer_hpp
    data_object to_d2o(const d2o_writer & file)
    {
        data_object data;
        data.first = file.get_class_id_for_type<achievement_objective>();
        release(id, data.second["id"], file);
        release(achievement_id, data.second["achievementId"], file);
        release(name_id, data.second["nameId"], file);
        release(criterion, data.second["criterion"], file);
        return data;
    }
#endif
}
#endif